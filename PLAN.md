# prompt-promote

将用户非标准格式的提示词在内部加工为结构化标准提示词并直接执行，忠实覆盖用户意图，单程、低开销的 PenguinHarness skill。

## 已确认设计决策

| # | 决策 | 说明 |
|---|------|------|
| 1 | 形态：skill | 一份 `SKILL.md`，装到 agent 上内联使用 |
| 2 | 全自动 | 优化后的提示词不展示给用户，模型内部消化后直接执行 |
| 3 | 单程加工 | 一次加工 → 执行；无多程迭代 |
| 4 | 成本/延迟敏感 | 不显著增加等待时间；自检嵌入同一次推理的思考阶段，不增加 API 往返 |
| 5 | 标记级双语 | 结构字段中英双写；内容语言跟随用户，不翻译 |
| 6 | 意图边界 | 意图 = 契约：全部覆盖、不超范围；措辞/结构/合理默认值可动 |
| 7 | debug 模式 | 默认关；开启时打印内部加工结果（开发与信任建立） |
| 8 | 模板来源 | 行业共识起草 v0.1（CO-STAR / CRISPE / RTF 交集），评测数据迭代 |
| 9 | 评测 | 端到端：脏提示词 → 理想结果；先小规模（10-20 用例） |
| 10 | 模板形态 | 轻量任务分类（写作/代码/分析…各自骨架），规则优先、模型兜底，成本接近通用模板 |

## 路线图

1. ✅ 收集种子用例（13 条，我拟 8 + 用户真实 5）
2. ✅ 测基线（干净 4.85 / 污染 4.65；污染精准触发 C09/C11 忠实类失败 → 确定 skill 战场）
3. ✅ 起草模板 v0.1 + SKILL.md
4. ✅ 测加工后效果（A/B：污染 4.65 → 加工后 4.92，C09/C11 失败被修复）
5. ✅ 按失败案例迭代（v0.1.1 已修协议泄漏 + 事实边界收紧；回归验证通过，见 ab-test.md）
6. 🔄 开源发布（发布准备已完成：README/LICENSE(MIT)/examples/评测集整理/tests-README/git 初始提交；**待发布到 Gitee 主发 + GitHub 镜像——发布前需用户提供账号凭据**）

## 外部测试集调研（2026-08-08）

结论：**无现成基准直接对应"脏提示词 → 标准提示词"管线**；需组合使用，详见 `docs/test-sets-survey.md`。

- A 组·约束遵循：IFEval（ModelScope 镜像 ✅）/ IFBench / FollowBench / ComplexConstraints / Inverse IFEval（中英双语 ✅）
- B 组·真实脏提示词：WildBench / WildChat / LMSYS-Chat-1M（抽样作端到端输入）
- C 组·事实边界：HaluEval / TruthfulQA / FActScore
- 保留并扩展自建用例 C01–C13（"内容信任"型污染为独有测试点）
- 网络：arXiv / ModelScope / BAAI 可达；HF / GitHub 被墙需镜像

## 外部评测 pilot（2026-08-08，`tests/external-eval.md`）

14 条外部用例（IFEval 2 / FollowBench 中文 3 / WildBench v2 5 / TruthfulQA 4），基线 vs 加工后同口径打分：

- **平均 3.43 → 4.54（+1.11）**；A 组约束遵循 +1.70 提升最大，B 组真实脏提示词 +1.00，C 组事实边界 +0.50
- 外部数据复现内部教训：B5（编造报告外收益）与 C09/C11 同型，加工后修复
- 明确能力边界：C4（广告声称当事实）无改善——skill 不提供知识，只约束来源外添加
- 数据获取已验证：ModelScope IFEval、hf-mirror WildBench/FollowBench 中文/TruthfulQA；用例存 `tests/fixtures/external-eval-cases.jsonl`

## 外部评测扩展（2026-08-08，40 条，`tests/external-eval-expanded.md`）

方法升级：独立 subagent 并行生成基线/加工后输出（同模型 deepseek-v4-flash）+ A 组机器打分 / B 组 checklist 通过率 / C 组答案匹配 / B9 程序化校验，修正 v1 自评偏差。

- **基线 4.86 → 加工后 4.96（+0.10）**；40 条中 6 条改善、34 条持平、0 条回退
- v1 的 +1.11 高估主要来自自评主观偏差；显式约束（IFEval）下基线本身全过
- skill 真实价值定位（数据支持）：①隐式/交互约束自检（A9 词序、B9 跨周边界连休——唯一可程序验证的改善）②多子任务完整性（B3/B4/B7/B8）③事实边界纪律（B16 占位符、C 组区分虚构）④执行结构清晰度；**不是**"格式化提示词"
- 能力边界：skill 不提供世界知识，C 组 TruthfulQA 对当前模型无区分度；事实边界更适合"源文+改写保真"构造（种子 C09/C11 已覆盖）
- 输出/用例存 `tests/fixtures/external-eval-cases-expanded.jsonl` 与 `tests/fixtures/outputs/`，可复现

## 总体评测结论（2026-08-08，`tests/overall-report.md`）

全部 4 轮评测汇总：对"脏提示词"总体提升 **+0.10 ~ +0.27**（取决于口径），0 回退。

- 脏会话（内容信任污染）：+0.27（4.65→4.92），忠实类失败 C09/C11 修复且回归通过——最显著、最可复现
- 真实用户脏提示词（WildBench）：+0.16（4.78→4.94，机器核对口径）
- 干净/规范提示词：几乎无增益（IFEval 基线全过、TruthfulQA 无区分度）
- 方法学：v1 自评 +1.11 高估；以 v2（subagent 生成 + 机器打分）为准
- 对外口径建议用 v2 数字并注明方法学修正

## 模板 v0.1 骨架（定稿基线）

**8 字段**：角色 / 任务 / 上下文 / 输入 / 约束 / 输出格式 / 示例 / 评判标准

- 标记级双语：字段名中英双写（如 `【角色】/ROLE`），内容语言跟随用户，不翻译用户材料
- 轻量任务分类：writing / coding / analysis / translation / other，规则优先、模型兜底
- 事实边界规则（污染基线教训）：
  1. 仅当前请求中明确陈述的内容是事实锚点
  2. 历史会话闲聊/背景仅供参考，不得作为事实依据
  3. 改写用户材料不得添加来源之外的信息（数据/技术栈/荣誉/理由），缺数据用占位符或索要

## 待定

- 安装目标 agent
- 评测评分细则
