---
name: prompt-promote
description: 将用户非标准格式的提示词在内部加工为结构化标准提示词后直接执行，忠实覆盖用户意图，单程、低开销。
short_description: Internally normalize messy prompts and execute faithfully in one pass.
short_description_zh: 内部规范化用户提示词并忠实执行，单程低开销。
version: 0.1.1
updated: 2026-08-08T23:30:00Z
---

# Prompt Promote

本 skill 是 agent 处理用户请求时的**行为协议**：每次收到用户提示词，先在内部加工为结构化标准提示词（**不展示给用户**），再忠实执行。核心目标是四句话：

> **意图全覆盖、范围不超出、约束不丢失、事实不虚构。**

## 何时使用

- 所有任务类用户请求默认应用（无需触发词）
- 例外：纯闲聊、问候、寒暄——直接回应，不加工

## 核心原则

1. **单程**：加工 → 自检 → 执行，一次完成，绝不迭代重跑
2. **忠实**：意图 = 契约。意图的全部必须覆盖（"可乐"不能缩成"饮料"），意图之外不得扩张（不能顺手"点汉堡"）；措辞、结构、合理默认值可以动
3. **事实边界**：只信当前请求中明确陈述的事实；历史闲聊不是事实依据；改写用户材料不得添加来源之外的信息
4. **成本**：自检嵌入思考阶段，不增加 API 往返；加工结果精简，不为凑字段
5. **双语**：结构字段中英双写（`【角色】/ROLE`），内容语言跟随用户，不翻译用户材料

## 工作流（单程五步）

### Step 1 解析 Parse（思考阶段）

提取四张清单：

- **意图清单**：主目标 + 子目标（用户到底要什么）
- **约束清单**：每条限制。注意附加约束最容易丢——"给我一杯可乐，不要太冰"里的"不要太冰"必须单独列出
- **事实锚点**：当前请求中明确陈述的事实（绑定，不可更改）
- **依赖检查**：是否引用前文材料/附件/URL（"刚才那段代码"→ 必须定位到具体来源；定位不到就反问）

### Step 2 分类 Classify

规则优先（见分类表），规则命中不了再由模型判断。选对应骨架。

### Step 3 加工 Normalize

用所选骨架在思考中构建内部结构化提示词（8 字段，见下）。补全规则：

- **非关键缺口** → 用合理默认值，内部标注 `默认假设：...`
- **关键缺口**（任务目标本身缺失，如"帮我写点东西"）→ 一次性反问，列全所有缺失项（写什么/给谁/什么形式）

### Step 4 自检 Self-check（思考阶段完成，无额外调用）

执行计划成型后逐条核对：

- [ ] **意图覆盖**：计划涵盖意图清单每一项？（可乐不能变饮料）
- [ ] **范围**：没有超出意图清单的动作？（不能顺手点汉堡）
- [ ] **约束**：约束清单每一条都有对应执行动作？（字数/语气/受众/禁止项）
- [ ] **事实边界**：是否添加了来源之外的事实？（技术栈/数据/理由/荣誉……来源没有的一律不添加）

### Step 5 执行 Execute

- 按加工后的提示词执行任务
- 只输出最终结果，**不展示加工后的提示词**
- **回复中不得提及内部加工、自检等过程**（如"我已按协议完成内部加工"这类话一律不说）
- 除非 debug 模式开启（见下）

## 事实边界规则（污染基线教训）

1. 只有当前请求中**明确陈述**的内容才是事实锚点
2. 历史会话中的闲聊/背景信息**仅供参考**，不得作为事实依据。反例：会话里闲聊提过"奶奶身体不好"，不等于请假理由就是奶奶生病——理由必须来自当前请求或向用户确认
3. 改写/优化用户材料时，**不得添加来源中没有的信息**（数据、技术栈、框架、荣誉、理由等）；来源未提及的内容一律不得推断后写入——缺数据/缺信息用占位符（如 `XX`）或向用户索要，绝不编造。**"合理推断"同样属于添加**：例如用户材料只写了"Java 后端开发"，不得推断为"用了 Spring Boot"并写入，应使用占位符或向用户确认

## 成本纪律

- 单程加工，禁止多轮迭代
- 自检在思考阶段完成，不发起额外模型调用
- 加工后的提示词保持精简：只为补关键缺口
- 预期额外开销：思考 +10~50 token、延迟 +0.1~0.5 秒

## 分类表

| 类型 | 规则触发词（命中即用） | 骨架 |
|------|------------------------|------|
| coding | 代码、脚本、bug、修复、正则、报错、接口、函数、爬虫、部署 | 代码类 |
| writing | 写、文案、邮件、文章、简历、小说、故事、方案、演讲稿 | 写作类 |
| analysis | 分析、对比、报告、趋势、数据、统计 | 分析类 |
| translation | 翻译、译成、翻成、translate | 翻译类 |
| other | 其余情况 | 通用 |

歧义处理："总结"有两种——面向表达的总结观点 → writing；面向数据的总结 → analysis，按上下文判断。

## 骨架（8 字段，标记级双语）

通用骨架：

```
【角色】ROLE          —— 以什么身份/视角执行
【任务】TASK          —— 动词开头的明确任务
【上下文】CONTEXT     —— 背景、前置条件、隐含假设
【输入】INPUT         —— 用户提供的材料/数据（原样保留）
【约束】CONSTRAINTS   —— 全部限制条件（逐条列出）
【输出格式】OUTPUT FORMAT —— 期望的形态/结构/长度
【示例】EXAMPLE       —— 有则给一个质量基准
【评判标准】CRITERIA  —— 怎样算完成得好（自检对照）
```

各类型骨架的填充差异见 `templates/README.md`（写作/代码/分析/翻译的字段侧重与示例）。

## 调试模式（默认关）

- 环境变量 `PROMPT_PROMOTE_DEBUG=1` 时：执行前把内部加工结果以"内部加工"小节输出，仅供开发调试
- 默认关闭：用户只看最终结果

## 示例

**用户消息**："帮我写一封给老板的请假邮件，语气要既尊重又随意，大概200字。"
**会话历史**：用户之前闲聊提过"我妈说我奶奶最近身体不太好……"

- Step 1 解析：意图=写请假邮件；约束=语气平衡、约200字、不虚构理由；事实锚点=无具体理由、无日期；依赖=无
- Step 2 分类：writing
- Step 3 加工：内部提示词含 `【约束】语气平衡/约200字/理由与日期用占位符，不得虚构` + `默认假设：请假理由未提供，用"私事"占位`
- Step 4 自检：计划里不能用"奶奶生病"当理由——那是历史闲聊，不是当前请求陈述的事实 ✅
- Step 5 执行：输出邮件，理由用占位符，语气"老板好：……"，字数约 150~200
